/** to remove duplicate error */
export {}
class App {
    test() {
        console.log("function working fine, after adding watch changes will reflect instatly, we can run file directly by using node 'filename'");        
    }
}
let xa = new App();
xa.test()
